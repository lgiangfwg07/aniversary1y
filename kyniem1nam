<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Kỷ niệm 1 năm yêu nhau của anh heo và em tôm</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&display=swap');
        body {
            font-family: 'Playfair Display', serif;
            background: linear-gradient(120deg, #ff9a9e, #fad0c4);
            margin: 0;
            padding: 0;
            text-align: center;
            color: #4a4a4a;
            overflow: hidden;
            position: relative;
        }
        .container {
            width: 80%;
            margin: 50px auto;
        }

        /* Hiệu ứng trái tim bay */
        @keyframes moveHearts {
            0% { transform: translateY(100vh) translateX(0); opacity: 1; }
            100% { transform: translateY(-10vh) translateX(20px); opacity: 0; }
        }

        .heart {
            position: absolute;
            width: 30px;
            height: 30px;
            background: url('https://cdn-icons-png.flaticon.com/512/833/833472.png') no-repeat center center/cover;
            animation: moveHearts 5s linear infinite;
        }

        h1 {
            font-family: 'Great Vibes', cursive;
            color: #ffd700;
            font-size: 4em;
            margin-bottom: 30px;
            text-shadow: 3px 3px 10px rgba(0, 0, 0, 0.3);
        }

        @keyframes float {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-20px); }
        }

        .letter-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 50px;
            padding: 50px 0;
        }

        .letter {
            background: rgba(255, 255, 255, 0.9);
            border-radius: 20px;
            padding: 30px;
            font-size: 1.5em;
            color: #d81b60;
            border: 3px solid #ff9a9e;
            cursor: pointer;
            transition: transform 0.5s ease-in-out;
            width: 90%;
            max-width: 1200px;
            box-shadow: 5px 5px 15px rgba(0,0,0,0.2);
            overflow: hidden;
            text-align: center;
            position: relative;
        }

        .letter:hover {
            transform: scale(1.05);
        }

        .letter::before, .letter::after {
            content: '';
            position: absolute;
            width: 40px;
            height: 40px;
            background: url('https://cdn-icons-png.flaticon.com/512/833/833472.png') no-repeat center center/cover;
            opacity: 0.5;
        }

        .letter::before {
            top: -20px;
            left: -20px;
        }

        .letter::after {
            bottom: -20px;
            right: -20px;
        }

        .letter-content {
            display: none;
            font-size: 1.2em;
            color: #4a4a4a;
            text-align: center;
            white-space: pre-wrap;
            line-height: 1.5;
        }

        .letter.opened .letter-content {
            display: block;
            animation: fadeInText 1s forwards;
        }

        @keyframes fadeInText {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        /* Modal CSS */
        .modal {
            display: none;
            position: fixed;
            z-index: 1000;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.5);
            justify-content: center;
            align-items: center;
            animation: fadeIn 0.3s ease-in-out;
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        .modal-content {
            background-color: #fff;
            padding: 30px;
            border-radius: 20px;
            width: 500px;
            text-align: center;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
            animation: slideIn 0.3s ease-in-out;
        }

        @keyframes slideIn {
            from { transform: translateY(-20px); opacity: 0; }
            to { transform: translateY(0); opacity: 1; }
        }

        .close {
            float: right;
            cursor: pointer;
            font-size: 24px;
            color: #d81b60;
            transition: transform 0.2s ease-in-out;
        }

        .close:hover {
            transform: scale(1.2);
        }

        #questionText {
            font-family: 'Quicksand', sans-serif;
            font-size: 1.8em;
            color: #d81b60;
            margin-bottom: 20px;
        }

        #answerInput {
            width: 100%;
            padding: 15px;
            margin-top: 20px;
            border: 2px solid #ff9a9e;
            border-radius: 10px;
            font-size: 1.2em;
            transition: border-color 0.3s ease-in-out;
        }

        #answerInput:focus {
            border-color: #d81b60;
            outline: none;
        }

        #submitAnswer {
            margin-top: 20px;
            padding: 15px 30px;
            background-color: #d81b60;
            color: #fff;
            border: none;
            border-radius: 10px;
            cursor: pointer;
            font-size: 1.2em;
            transition: background-color 0.3s ease-in-out, transform 0.2s ease-in-out;
        }

        #submitAnswer:hover {
            background-color: #ff9a9e;
            transform: scale(1.05);
        }

        #submitAnswer:active {
            transform: scale(0.95);
        }

        #errorMessage {
            color: red;
            display: none;
            margin-top: 15px;
            font-size: 1.2em;
            animation: shake 0.5s ease-in-out;
        }

        @keyframes shake {
            0%, 100% { transform: translateX(0); }
            25% { transform: translateX(-10px); }
            50% { transform: translateX(10px); }
            75% { transform: translateX(-10px); }
        }

        /* Nút cuộn lên và cuộn xuống */
        #scrollToTop, #scrollToBottom {
            position: fixed;
            right: 20px;
            padding: 10px;
            background-color: #d81b60;
            color: white;
            border: none;
            border-radius: 50%;
            cursor: pointer;
            font-size: 18px;
            width: 50px;
            height: 50px;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
            transition: background-color 0.3s ease-in-out, transform 0.2s ease-in-out;
        }

        #scrollToTop {
            bottom: 100px;
        }

        #scrollToBottom {
            bottom: 30px;
        }

        #scrollToTop:hover, #scrollToBottom:hover {
            background-color: #ff9a9e;
            transform: scale(1.1);
        }

        #scrollToTop:active, #scrollToBottom:active {
            transform: scale(0.95);
        }

        /* Cuộn mượt */
        html {
            scroll-behavior: smooth;
        }
    </style>
</head>
<body>
    <!-- Hiệu ứng trái tim bay trên background -->
    <div id="hearts-background"></div>

    <h1>Kỷ niệm 1 năm yêu nhau của anh heo và em tôm 💖</h1>
    <div class="letter-container">
        <div class="letter" data-question="Chúng mình yêu nhau từ khi nào?" data-answer="23/02/2024">
            Bức thư 1
            <div class="letter-content">
                Ngày đầu tiên anh heo quen em tôm, mặc dù rất ngại nhưng mà vẫn muốn nói chuyện với em tôm.
            </div>
        </div>
        <div class="letter" data-question="Ngày sinh nhật của anh?" data-answer="14/02/2007">
            Bức thư 2
            <div class="letter-content">
                24/02/2024 - Sinh nhật đầu tiên cùng em, một ngày đáng nhớ khi anh gửi lời chúc cho em tuy còn rất ngại.
            </div>
        </div>
        <div class="letter" data-question="Món quà đầu tiên anh tặng em?" data-answer="một bó hoa">
            Bức thư 3
            <div class="letter-content">
                14/02/2025 - Valentine đầu tiên và mong rằng sẽ còn nhiều Valentine hạnh phúc bên nhau.
            </div>
        </div>
        <div class="letter" data-question="Anh tặng quà cho em đầu tiên dịp nào?" data-answer="8/3">
            Bức thư 4
            <div class="letter-content">
                23/02/2025 - Tròn 1 năm yêu nhau một năm bên nhau, cùng sẻ chia hạnh phúc và vượt qua nhiều rào cản để hiểu nhau hơn.
            </div>
        </div>
        <div class="letter" data-question="Nguyện vọng 1 của anh là gì?" data-answer="HCMUS">
            Bức thư 5
            <div class="letter-content">
                24/02/2025 - Sinh nhật lần 2 cùng em, chúc em mãi xinh đẹp, có tất cà vả mãi là tình yêu của anh.
            </div>
        </div>
    </div>

    <!-- Modal -->
    <div id="questionModal" class="modal">
        <div class="modal-content">
            <span class="close">&times;</span>
            <p id="questionText"></p>
            <input type="text" id="answerInput" placeholder="Nhập đáp án của bạn...">
            <button id="submitAnswer">Gửi</button>
            <p id="errorMessage">Sai rồi, hãy thử lại!</p>
        </div>
    </div>

    <!-- Nút cuộn lên và cuộn xuống -->
    <button id="scrollToTop" title="Lên đầu trang">↑</button>
    <button id="scrollToBottom" title="Xuống cuối trang">↓</button>

    <script>
        const modal = document.getElementById('questionModal');
        const questionText = document.getElementById('questionText');
        const answerInput = document.getElementById('answerInput');
        const submitAnswer = document.getElementById('submitAnswer');
        const errorMessage = document.getElementById('errorMessage');
        const closeModal = document.querySelector('.close');

        let currentLetter = null;

        // Mở modal khi nhấp vào thư
        document.querySelectorAll('.letter').forEach(letter => {
            letter.addEventListener('click', () => {
                if (letter.classList.contains('opened')) {
                    letter.classList.remove('opened');
                    return;
                }

                const question = letter.getAttribute('data-question');
                const answer = letter.getAttribute('data-answer');

                if (answer === "any") {
                    letter.classList.add('opened');
                    return;
                }

                currentLetter = letter;
                questionText.textContent = question;
                modal.style.display = 'flex';
                errorMessage.style.display = 'none';
                answerInput.value = '';
            });
        });

        // Đóng modal
        closeModal.addEventListener('click', () => {
            modal.style.display = 'none';
        });

        // Xử lý khi nhấn nút Gửi
        submitAnswer.addEventListener('click', () => {
            const userAnswer = answerInput.value.trim();
            const correctAnswer = currentLetter.getAttribute('data-answer');

            if (correctAnswer === "any" || userAnswer === correctAnswer) {
                currentLetter.classList.add('opened');
                modal.style.display = 'none';
            } else {
                errorMessage.style.display = 'block';
            }
        });

        // Tạo hiệu ứng trái tim bay
        const heartsBackground = document.getElementById('hearts-background');
        function createHeart() {
            const heart = document.createElement('div');
            heart.classList.add('heart');
            heart.style.left = Math.random() * 100 + 'vw';
            heart.style.animationDuration = Math.random() * 3 + 2 + 's';
            heartsBackground.appendChild(heart);

            // Xóa trái tim sau khi bay xong
            setTimeout(() => {
                heart.remove();
            }, 5000);
        }

        // Tạo trái tim liên tục
        setInterval(createHeart, 300);

        // Xử lý nút cuộn lên
        const scrollToTopBtn = document.getElementById('scrollToTop');
        scrollToTopBtn.addEventListener('click', () => {
            window.scrollTo({
                top: 0,
                behavior: 'smooth'
            });
        });

        // Xử lý nút cuộn xuống
        const scrollToBottomBtn = document.getElementById('scrollToBottom');
        scrollToBottomBtn.addEventListener('click', () => {
            window.scrollTo({
                top: document.body.scrollHeight,
                behavior: 'smooth'
            });
        });

        // Cuộn mượt bằng chuột
        const scrollSpeed = 100;
        window.addEventListener("wheel", (event) => {
            event.preventDefault(); // Ngăn hành vi cuộn mặc định
            const delta = event.deltaY; // Lấy hướng cuộn (lên hoặc xuống)
            window.scrollBy({
                top: delta * (scrollSpeed / 100), // Tăng/giảm tốc độ cuộn
                behavior: "smooth" // Cuộn mượt
            });
        });
    </script>
    <div class="music-player">
        <iframe width="1000" height="700" src="https://www.youtube.com/embed/PNhYz6RmIr4?autoplay=1&loop=1&playlist=PNhYz6RmIr4"></iframe>
    </div>
</body>
</html>